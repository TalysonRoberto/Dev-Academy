---
id: "css-boxmodel-01"
conteudoId: "css"
topicoId: "box-model"
titulo: "Box Model (margin, padding, border)"
xp: 40
duracaoEstimada: "15min"
exercicio:
  enunciado: "Ajuste espaçamentos de uma lista de cards usando o box model (margin, padding, border)."
  linguagem: "html"
  codigoInicial: "<!-- Ajuste os espaçamentos dos cards -->\n<style>\n  .card { /* Adicione estilos aqui */ }\n</style>\n\n<div class=\"card\">Card 1</div>\n<div class=\"card\">Card 2</div>\n<div class=\"card\">Card 3</div>"
  validationCode: "const hasMargin = code.includes('margin');
const hasPadding = code.includes('padding');
const hasBorder = code.includes('border');
return hasMargin && hasPadding && hasBorder;"
  testDescription: "Verifica se usou margin, padding e border"
---

## Introdução

Todo elemento HTML é uma caixa com 4 camadas: conteúdo, padding, border e margin.

## Conceito Explicado

```
┌─────────────────────────────┐
│          MARGIN             │ ← Espaço externo
│  ┌───────────────────────┐  │
│  │       BORDER          │  │ ← Borda
│  │  ┌─────────────────┐  │  │
│  │  │    PADDING      │  │  │ ← Espaço interno
│  │  │  ┌───────────┐  │  │  │
│  │  │  │ CONTEÚDO  │  │  │  │
│  │  │  └───────────┘  │  │  │
│  │  └─────────────────┘  │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

### Propriedades
```css
margin: 10px;              /* Todos os lados */
margin: 10px 20px;         /* Vertical | Horizontal */
padding: 16px;
border: 1px solid #ccc;
border-radius: 8px;
```

## Exemplo Visual

```css
.card {
  margin: 16px;
  padding: 20px;
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  background: white;
}
```

**Preview:** Cards com espaçamento externo (margin), interno (padding) e borda arredondada.

## Boas Práticas

- ✅ Use `padding` para espaço dentro do elemento
- ✅ Use `margin` para espaço entre elementos
- ✅ Use `border-radius` para cantos arredondados
- ❌ Não use `margin` para espaço interno

## Atividade Final

Ajuste os cards fornecidos:
1. Adicione `margin: 16px` entre os cards
2. Adicione `padding: 20px` dentro dos cards
3. Adicione `border: 1px solid #ccc` nos cards
